# client_id 一般放 env（半公開，前端 SDK 本來就看得到）。
# 目前這組是從舊專案借來的 sandbox 憑證，不屬於本服務專屬的 app，
# 所以暫時一併放 Secret Manager —— public repo 的 git 歷史刪不掉。
# 換成專屬 app 後，把 PAYPAL_CLIENT_ID 移回 .cicd/env.dev。
PAYPAL_CLIENT_ID=payment-paypal-client-id-dev:latest
PAYPAL_CLIENT_SECRET=payment-paypal-client-secret-dev:latest
