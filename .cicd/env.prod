LOG_LEVEL=info
PAYPAL_ENV=live

# 專屬的 live app「adamsourceinfo-payments-live」（商家帳號 adamsourceinfo@gmail.com）。
# client id 是半公開的，放環境變數即可；secret 在 Secret Manager。
PAYPAL_CLIENT_ID=BAATbKOVsXzMQvVZkvYLnXJlTPM9Q-p1CR-mMlY96Fsw-Nw7ohj6hHJ4u2oqDehVSytFhHwfrLCGOPlkAs

# 尚未註冊 —— 雞生蛋：要先部署 prod 拿到 Cloud Run URL 才註冊得了。
# 缺這一行時服務會正常啟動但降級：/health 回 webhook: unconfigured、
# POST /v1/webhooks 回 503（不會靜靜收下無法驗簽的請求）。
# PAYPAL_WEBHOOK_ID=
